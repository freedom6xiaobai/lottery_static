const menuService = require('../services/menuService_db');/** * 获取所有菜单（管理员专用） */exports.getList = (req, res) => {  try {    const data = menuService.getAll();    res.json({      success: true,      data: data,      code: 200    });  } catch (err) {    res.status(500).json({      success: false,      message: 'Failed to fetch menus',      error: err.message,      code: 500    });  }};/** * 获取当前用户可访问的菜单 */exports.getMyMenus = (req, res) => {  try {    const permissions = req.user.permissions || [];    const data = menuService.getByPermissions(permissions);    res.json({      success: true,      data: data,      code: 200    });  } catch (err) {    res.status(500).json({      success: false,      message: 'Failed to fetch menus',      error: err.message,      code: 500    });  }};/** * 获取单个菜单 */exports.getById = (req, res) => {  try {    const { id } = req.params;    const menu = menuService.getById(parseInt(id));    if (!menu) {      return res.status(404).json({        success: false,        message: 'Menu not found',        code: 404      });    }    res.json({      success: true,      data: menu,      code: 200    });  } catch (err) {    res.status(500).json({      success: false,      message: 'Failed to fetch menu',      error: err.message,      code: 500    });  }};/** * 创建菜单 */exports.create = (req, res) => {  try {    const { name, path, icon, permission, component, children } = req.body;    if (!name) {      return res.status(400).json({        success: false,        message: 'Menu name is required',        code: 400      });    }    const menu = menuService.create({      name,      path,      icon,      permission,      component,      children    });    res.status(201).json({      success: true,      data: menu,      code: 201    });  } catch (err) {    res.status(500).json({      success: false,      message: 'Failed to create menu',      error: err.message,      code: 500    });  }};/** * 更新菜单 */exports.update = (req, res) => {  try {    const { id } = req.params;    const { name, path, icon, permission, component, children } = req.body;    const menu = menuService.update(parseInt(id), {      name,      path,      icon,      permission,      component,      children    });    if (!menu) {      return res.status(404).json({        success: false,        message: 'Menu not found',        code: 404      });    }    res.json({      success: true,      data: menu,      code: 200    });  } catch (err) {    res.status(500).json({      success: false,      message: 'Failed to update menu',      error: err.message,      code: 500    });  }};/** * 删除菜单 */exports.delete = (req, res) => {  try {    const { id } = req.params;    const menu = menuService.delete(parseInt(id));    if (!menu) {      return res.status(404).json({        success: false,        message: 'Menu not found',        code: 404      });    }    res.json({      success: true,      data: menu,      code: 200    });  } catch (err) {    res.status(500).json({      success: false,      message: 'Failed to delete menu',      error: err.message,      code: 500    });  }};