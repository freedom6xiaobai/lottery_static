const roleService = require('../services/roleService_db');/** * 获取角色列表 */exports.getList = (req, res) => {  try {    const data = roleService.getAll();    res.json({      success: true,      data: data,      code: 200    });  } catch (err) {    res.status(500).json({      success: false,      message: 'Failed to fetch roles',      error: err.message,      code: 500    });  }};/** * 获取单个角色 */exports.getById = (req, res) => {  try {    const { id } = req.params;    const role = roleService.getById(parseInt(id));    if (!role) {      return res.status(404).json({        success: false,        message: 'Role not found',        code: 404      });    }    res.json({      success: true,      data: role,      code: 200    });  } catch (err) {    res.status(500).json({      success: false,      message: 'Failed to fetch role',      error: err.message,      code: 500    });  }};/** * 创建角色 */exports.create = (req, res) => {  try {    const { name, description, permissions } = req.body;    if (!name) {      return res.status(400).json({        success: false,        message: 'Role name is required',        code: 400      });    }    const role = roleService.create({      name,      description,      permissions    });    res.status(201).json({      success: true,      data: role,      code: 201    });  } catch (err) {    res.status(500).json({      success: false,      message: 'Failed to create role',      error: err.message,      code: 500    });  }};/** * 更新角色 */exports.update = (req, res) => {  try {    const { id } = req.params;    const { name, description, permissions } = req.body;    const role = roleService.update(parseInt(id), {      name,      description,      permissions    });    if (!role) {      return res.status(404).json({        success: false,        message: 'Role not found',        code: 404      });    }    res.json({      success: true,      data: role,      code: 200    });  } catch (err) {    res.status(500).json({      success: false,      message: 'Failed to update role',      error: err.message,      code: 500    });  }};/** * 删除角色 */exports.delete = (req, res) => {  try {    const { id } = req.params;    const role = roleService.delete(parseInt(id));    if (!role) {      return res.status(404).json({        success: false,        message: 'Role not found',        code: 404      });    }    res.json({      success: true,      data: role,      code: 200    });  } catch (err) {    res.status(500).json({      success: false,      message: 'Failed to delete role',      error: err.message,      code: 500    });  }};/** * 添加权限到角色 */exports.addPermission = (req, res) => {  try {    const { id } = req.params;    const { permission } = req.body;    if (!permission) {      return res.status(400).json({        success: false,        message: 'Permission is required',        code: 400      });    }    const role = roleService.addPermission(parseInt(id), permission);    if (!role) {      return res.status(404).json({        success: false,        message: 'Role not found',        code: 404      });    }    res.json({      success: true,      data: role,      code: 200    });  } catch (err) {    res.status(500).json({      success: false,      message: 'Failed to add permission',      error: err.message,      code: 500    });  }};/** * 从角色删除权限 */exports.removePermission = (req, res) => {  try {    const { id } = req.params;    const { permission } = req.body;    if (!permission) {      return res.status(400).json({        success: false,        message: 'Permission is required',        code: 400      });    }    const role = roleService.removePermission(parseInt(id), permission);    if (!role) {      return res.status(404).json({        success: false,        message: 'Role not found',        code: 404      });    }    res.json({      success: true,      data: role,      code: 200    });  } catch (err) {    res.status(500).json({      success: false,      message: 'Failed to remove permission',      error: err.message,      code: 500    });  }};